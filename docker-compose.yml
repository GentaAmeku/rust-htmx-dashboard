services:
  app:
    build: .
    ports:
      - "3000:3000"
    environment:
      - DATABASE_URL=sqlite:/app/data/db.sqlite?mode=rwc
      - RUST_LOG=debug
    volumes:
      - db-data:/app/data

volumes:
  db-data:
